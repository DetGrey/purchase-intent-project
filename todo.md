
- Add Hyperparameter Tuning (GridSearchCV) - Use RandomizedSearchCV or GridSearchCV on your best model (Random Forest) to find the perfect settings

- Add Youden's Index to evaluate function instead of just adding it separately afterwards